# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# npm pack artifacts (release verification)
*.tgz

# Ama local index cache (file-backed SqliteStore for the dev HTTP server)
.ama/

# beads: commit issues.jsonl + the audit log, but never the local Dolt database
.beads/*.db
.beads/dolt/
.beads/.dolt/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Logs / coverage
*.log
coverage/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
